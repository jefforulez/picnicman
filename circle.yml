
# /home/ubuntu/.rvm/rubies/ruby-2.2.3/bin/
# /home/ubuntu/.rvm/gems/ruby-2.2.3@global/bin

dependencies:
    override:
        - /home/ubuntu/.rvm/gems/ruby-2.2.3@global/bin/bundle install
        - sudo pip install awscli

test:
    override:
        - /home/ubuntu/.rvm/gems/ruby-2.2.3@global/bin/bundle exec jekyll build
        - /home/ubuntu/.rvm/gems/ruby-2.2.3@global/bin/bundle exec htmlproof

deployment:

    release:
        branch: master
        commands:
            - aws s3 sync _site s3://www.picnicman.com/
