
machine:
  ruby:
    version: rbx-2.2.3

dependencies:
    override:
        - bundle install
        - sudo pip install awscli

test:
    override:
        - bash ./build_site.sh
        - bundle exec htmlproof

deployment:

    release:
        branch: master
        commands:
            - aws s3 sync _site s3://www.picnicman.com/
